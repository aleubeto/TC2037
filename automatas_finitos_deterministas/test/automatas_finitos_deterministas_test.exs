defmodule AutomatasFinitosDeterministasTest do
  use ExUnit.Case
  doctest AutomatasFinitosDeterministas

  test "greets the world" do
    assert AutomatasFinitosDeterministas.hello() == :world
  end
end
